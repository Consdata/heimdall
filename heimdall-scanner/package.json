{
  "name": "@consdata/heimdall-exporter",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Grzegorz Lipecki",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prebuild": "rimraf .build && rimraf dist",
    "postbuild": "rimraf .build",
    "build:compile": "tsc -p src/tsconfig.json",
    "build": "yarn build:compile",
    "release": "yarn build && yarn publish --minor --non-interactive --access public && git push && git push --tags"
  },
  "dependencies": {
    "@yarnpkg/lockfile": "^1.1.0",
    "file-system": "^2.2.2"
  },
  "devDependencies": {
    "@types/node": "^12.0.0",
    "ts-node": "^8.1.0",
    "typescript": "^3.4.5",
    "rimraf": "2.6.3",
    "concurrently": "4.1.0",
    "copyfiles": "2.1.0"
  }
}
