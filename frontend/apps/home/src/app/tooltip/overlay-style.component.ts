import {Component, ViewEncapsulation} from "@angular/core";

@Component({
  selector: 'hmd-overlay-styles',
  template: '',
  styleUrls: ["overlay-style.component.scss"],
  encapsulation: ViewEncapsulation.None
})
export class OverlayStyleComponent {
  // only for import css without encapsulation
}
